# Atrio React Frontend

Atrio - Student Attendance Management System.

## Stack

React, Vite, TypeScript, Tailwind CSS, React Router, Axios.

## Setup

```bash
npm install
copy .env.example .env
npm run dev
```

The API is expected at `http://localhost:5289` (see `.env.example`).
