import { BookOpen, Users } from 'lucide-react'
import { useCallback, useEffect, useState } from 'react'
import { ErrorBanner, LoadingState, errorMessage } from '../components/common/AsyncState'
import { PageHeader } from '../components/common/PageHeader'
import apiClient from '../services/api'
import type { ClassDto } from '../types'
export function Classes() { const [classes, setClasses] = useState<ClassDto[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const load = useCallback(async () => { setLoading(true); setError(''); try { setClasses((await apiClient.get<ClassDto[]>('/classes')).data) } catch (e) { setError(errorMessage(e, 'Classes could not be loaded.')) } finally { setLoading(false) } }, []); useEffect(() => { void load() }, [load]); return <section><PageHeader title="Class management" description="Active courses and their live student rosters." />{error && <ErrorBanner message={error} retry={() => void load()} />}{loading ? <LoadingState label="Loading classes..." /> : <div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{classes.map(course => <article key={course.id} className="glass rounded-2xl p-5"><div className="rounded-xl bg-indigo-500/15 p-3 text-indigo-300 w-fit"><BookOpen size={22} /></div><h2 className="mt-5 font-semibold text-white">{course.name}</h2><p className="mt-1 text-sm text-slate-400">{course.code} · Academic year {course.academicYear}</p><div className="mt-6 flex items-center gap-2 border-t border-slate-700/50 pt-4 text-sm text-slate-300"><Users size={16} className="text-blue-300" />{course.studentCount} students</div></article>)}</div>}</section> }
