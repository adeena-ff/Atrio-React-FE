import { BookOpen, MoreHorizontal, Plus, Users } from 'lucide-react'
import { PageHeader } from '../components/common/PageHeader'
import { classes } from '../data/mockData'
export function Classes() { return <section><PageHeader title="Class management" description="Manage active courses and their student rosters." action={<button className="btn-primary"><Plus size={18} />Create class</button>} /><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{classes.map(course => <article key={course.id} className="glass rounded-2xl p-5"><div className="flex justify-between"><div className="rounded-xl bg-indigo-500/15 p-3 text-indigo-300"><BookOpen size={22} /></div><button className="text-slate-500 hover:text-white"><MoreHorizontal /></button></div><h2 className="mt-5 font-semibold text-white">{course.name}</h2><p className="mt-1 text-sm text-slate-400">{course.code} · Academic year {course.year}</p><div className="mt-6 flex items-center justify-between border-t border-slate-700/50 pt-4"><span className="inline-flex items-center gap-2 text-sm text-slate-300"><Users size={16} className="text-blue-300" />{course.students} students</span><span className="text-sm font-semibold text-emerald-300">{course.attendance}%</span></div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-slate-700"><div className="h-full rounded-full bg-emerald-500" style={{ width: `${course.attendance}%` }} /></div></article>)}</div></section> }
