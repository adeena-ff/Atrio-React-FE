import { AlertTriangle, ArrowUpRight, BookOpen, Users } from 'lucide-react'
import { useCallback, useEffect, useState } from 'react'
import { ErrorBanner, LoadingState, errorMessage } from '../components/common/AsyncState'
import { PageHeader } from '../components/common/PageHeader'
import apiClient from '../services/api'
import type { DashboardDto } from '../types'
const items = [{ key: 'totalStudents', label: 'Total Students', icon: Users, tone: 'text-indigo-300 bg-indigo-500/15' }, { key: 'todayAttendancePercentage', label: "Today's Attendance", icon: ArrowUpRight, tone: 'text-emerald-300 bg-emerald-500/15' }, { key: 'activeClasses', label: 'Active Classes', icon: BookOpen, tone: 'text-blue-300 bg-blue-500/15' }, { key: 'lowAttendanceAlerts', label: 'Needs Attention', icon: AlertTriangle, tone: 'text-amber-300 bg-amber-500/15' }] as const
export function Dashboard() { const [data, setData] = useState<DashboardDto>(); const [error, setError] = useState(''); const load = useCallback(async () => { setError(''); try { setData((await apiClient.get<DashboardDto>('/dashboard')).data) } catch (e) { setError(errorMessage(e, 'Dashboard data could not be loaded.')) } }, []); useEffect(() => { void load() }, [load]); return <section><PageHeader title="Attendance at a glance" description="Live overview from Atrio's attendance API." />{error && <ErrorBanner message={error} retry={() => void load()} />}{!data ? <LoadingState label="Loading dashboard..." /> : <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{items.map(({ key, label, icon: Icon, tone }) => { const value = data[key]; return <article key={key} className="glass rounded-2xl p-5"><div className="flex items-start justify-between"><div><p className="text-sm text-slate-400">{label}</p><p className="mt-2 text-3xl font-bold text-white">{key === 'todayAttendancePercentage' ? `${value}%` : value}</p></div><div className={`rounded-xl p-3 ${tone}`}><Icon size={20} /></div></div><p className="mt-4 text-xs text-slate-400">Updated from the live database</p></article>})}</div>}</section> }
