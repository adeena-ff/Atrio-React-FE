import { Plus, Search, X } from 'lucide-react'
import { useMemo, useState } from 'react'
import { PageHeader } from '../components/common/PageHeader'
import { students, statusStyles } from '../data/mockData'
export function Students() { const [query, setQuery] = useState(''); const [showModal, setShowModal] = useState(false); const [submitted, setSubmitted] = useState(false); const filtered = useMemo(() => students.filter(s => `${s.name} ${s.enrollment} ${s.className}`.toLowerCase().includes(query.toLowerCase())), [query]); return <section><PageHeader title="Student management" description="Search, review, and add student records." action={<button className="btn-primary" onClick={() => setShowModal(true)}><Plus size={18} />Add student</button>} /><div className="glass overflow-hidden rounded-2xl"><div className="flex flex-col gap-3 border-b border-slate-700/50 p-4 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 text-slate-500" size={18} /><input className="field pl-10" placeholder="Search by name, enrollment or class" value={query} onChange={e => setQuery(e.target.value)} /></div><select className="field sm:w-44"><option>All classes</option><option>Mathematics 10A</option><option>Computer Science</option></select></div><div className="overflow-x-auto"><table className="w-full min-w-180 text-left text-sm"><thead className="bg-slate-950/35 text-xs uppercase text-slate-500"><tr><th className="px-5 py-4">Student</th><th>Enrollment</th><th>Class</th><th>Attendance</th><th>Status today</th></tr></thead><tbody>{filtered.map(s => <tr key={s.id} className="border-t border-slate-700/40 text-slate-300"><td className="px-5 py-4 font-medium text-white">{s.name}</td><td className="text-slate-400">{s.enrollment}</td><td>{s.className}</td><td><span className={s.attendance < 85 ? 'text-rose-300' : 'text-emerald-300'}>{s.attendance}%</span></td><td><span className={`rounded-full border px-2.5 py-1 text-xs ${statusStyles[s.status as keyof typeof statusStyles]}`}>{s.status}</span></td></tr>)}</tbody></table></div></div>{showModal && <div className="fixed inset-0 z-30 grid place-items-center bg-slate-950/75 p-4"><form onSubmit={e => { e.preventDefault(); setSubmitted(true) }} className="glass relative w-full max-w-lg rounded-2xl p-6"><button type="button" onClick={() => setShowModal(false)} className="absolute right-4 top-4 text-slate-400"><X /></button><h2 className="text-xl font-bold text-white">Add a student</h2><p className="mb-5 text-sm text-slate-400">Create a mock record for UI testing.</p><div className="grid gap-3 sm:grid-cols-2">{['First name', 'Last name', 'Email address', 'Enrollment number'].map(label => <input key={label} required className="field" placeholder={label} />)}</div><select required className="field mt-3"><option value="">Choose a class</option><option>Mathematics 10A</option><option>Computer Science</option></select>{submitted && <p className="mt-3 text-sm text-emerald-300">Mock student validated successfully.</p>}<div className="mt-6 flex justify-end gap-2"><button type="button" className="btn-secondary" onClick={() => setShowModal(false)}>Cancel</button><button className="btn-primary">Save student</button></div></form></div>}</section> }
