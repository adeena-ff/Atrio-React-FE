import { Plus, Search, X } from 'lucide-react'
import { useCallback, useEffect, useState, type FormEvent } from 'react'
import { ErrorBanner, LoadingState, errorMessage } from '../components/common/AsyncState'
import { PageHeader } from '../components/common/PageHeader'
import apiClient from '../services/api'
import type { ClassDto, CreateStudentDto, StudentDto } from '../types'
export function Students() { const [students, setStudents] = useState<StudentDto[]>([]); const [classes, setClasses] = useState<ClassDto[]>([]); const [query, setQuery] = useState(''); const [classId, setClassId] = useState(''); const [showModal, setShowModal] = useState(false); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [form, setForm] = useState<CreateStudentDto>({ firstName: '', lastName: '', email: '', enrollmentNumber: '', classId: '' }); const load = useCallback(async () => { setLoading(true); setError(''); try { const [studentResult, classResult] = await Promise.all([apiClient.get<StudentDto[]>('/students', { params: { search: query || undefined, classId: classId || undefined } }), apiClient.get<ClassDto[]>('/classes')]); setStudents(studentResult.data); setClasses(classResult.data) } catch (e) { setError(errorMessage(e, 'Students could not be loaded.')) } finally { setLoading(false) } }, [query, classId]); useEffect(() => { const timer = window.setTimeout(() => void load(), 250); return () => window.clearTimeout(timer) }, [load]); const submit = async (event: FormEvent) => { event.preventDefault(); setError(''); try { await apiClient.post('/students', form); setShowModal(false); setForm({ firstName: '', lastName: '', email: '', enrollmentNumber: '', classId: '' }); await load() } catch (e) { setError(errorMessage(e, 'Student could not be created.')) } }; return <section><PageHeader title="Student management" description="Search, review, and add live student records." action={<button className="btn-primary" onClick={() => setShowModal(true)}><Plus size={18} />Add student</button>} />{error && <ErrorBanner message={error} retry={() => void load()} />}{loading ? <LoadingState label="Loading students..." /> : <div className="glass overflow-hidden rounded-2xl"><div className="flex flex-col gap-3 border-b border-slate-700/50 p-4 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 text-slate-500" size={18} /><input className="field pl-10" placeholder="Search by name, enrollment or email" value={query} onChange={e => setQuery(e.target.value)} /></div><select className="field sm:w-56" value={classId} onChange={e => setClassId(e.target.value)}><option value="">All classes</option>{classes.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="overflow-x-auto"><table className="w-full min-w-180 text-left text-sm"><thead className="bg-slate-950/35 text-xs uppercase text-slate-500"><tr><th className="px-5 py-4">Student</th><th>Enrollment</th><th>Class</th><th>Attendance</th></tr></thead><tbody>{students.map(s => <tr key={s.id} className="border-t border-slate-700/40 text-slate-300"><td className="px-5 py-4 font-medium text-white">{s.firstName} {s.lastName}</td><td>{s.enrollmentNumber}</td><td>{s.className}</td><td className={s.attendancePercentage < 75 ? 'text-rose-300' : 'text-emerald-300'}>{s.attendancePercentage}%</td></tr>)}</tbody></table></div></div>}{showModal && <div className="fixed inset-0 z-30 grid place-items-center bg-slate-950/75 p-4"><form onSubmit={submit} className="glass relative w-full max-w-lg rounded-2xl p-6"><button type="button" onClick={() => setShowModal(false)} className="absolute right-4 top-4 text-slate-400"><X /></button><h2 className="text-xl font-bold text-white">Add a student</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{(['firstName', 'lastName', 'email', 'enrollmentNumber'] as const).map(key => <input key={key} required className="field" type={key === 'email' ? 'email' : 'text'} placeholder={key === 'enrollmentNumber' ? 'Enrollment number' : key === 'firstName' ? 'First name' : key === 'lastName' ? 'Last name' : 'Email address'} value={form[key]} onChange={e => setForm({ ...form, [key]: e.target.value })} />)}</div><select required className="field mt-3" value={form.classId} onChange={e => setForm({ ...form, classId: e.target.value })}><option value="">Choose a class</option>{classes.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select><div className="mt-6 flex justify-end gap-2"><button type="button" className="btn-secondary" onClick={() => setShowModal(false)}>Cancel</button><button className="btn-primary">Save student</button></div></form></div>}</section> }
